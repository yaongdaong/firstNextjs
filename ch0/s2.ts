/* 타입스크립트 (TypeScript) - 정적 타이핑 (Static Typing)
 * 특징: 변수 선언 시 타입을 명시함. 잘못된 타입 대입 시 컴파일 단계에서 에러 발생.*/
let message: string = "Hello, TypeScript"; // 문자열만 가능
let count: number = 100; // 숫자만 가능
let isFinished: boolean = false; // 불리언만 가능

// r1
// 타입스크립트:정적타이핑(변수선언시타입명시,컴파일타임에타입지정됨)
let message1: string = "Hello, TypeScript";
let count1: number = 100;
let isFinished1: boolean = false;

// r2
// 타입스크립트:정적타이핑(변수선언시타입명시,컴파일타임에타입지정됨)
let message2: string = "Hello, TypeScript";
let count2: number = 100;
let isFinished2: boolean = false;

// r3
// 타입스크립트:정적타이핑(변수선언시타입명시,컴파일타임에타입지정됨)
let messgae3: string = "Hello,TypeScript";
let count3: number = 100;
let isFinished3: boolean = false;

// r4
// 타입스크립트:정적타이핑(변수선언시타입명시,컴파일타임에타입지정됨)
let message4: string = "Hello,TypeScript";
let count4: number = 10;
let isFinished4: boolean = false;

// r5
// 타입스크립트:정적타이핑(변수선언시타입명시,컴파일타임에타입지정됨)
let message5: string = "Hello,TypeScript";
let count5: number = 10;
let isFinished5: boolean = false;
